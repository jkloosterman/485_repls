function adder(x) {
  return function(y) {
      return x + y;
   }
}

// Part 1: Use adder to create a function which adds one to its input
let addOne = ________________;
// Print 11 to the console
console.log(addOne(____________));

// Part 2: Use adder to create a function addTwo that adds two to its input
//   and call it

// your code here
